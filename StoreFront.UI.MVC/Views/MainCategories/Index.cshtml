@model IEnumerable<SF.DATA.EF.MainCategory>

@{
    ViewBag.Title = "Main Categories";
}

@section styles {
    <link href="//cdn.datatables.net/1.10.22/css/jquery.dataTables.min.css" rel="stylesheet" />
}

<hr />

<div class="container">
    <h2>@ViewBag.Title</h2>

    <p>
        @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-outline-warning" })
    </p>
    <table class="table table-hover table-borderless">
        <thead class="thead-light">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.MainCategoryName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MainDescription)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.MainCategoryName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MainDescription)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.MainCategoryID }) |
                        @Html.ActionLink("Details", "Details", new { id = item.MainCategoryID }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.MainCategoryID })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section scripts {
    <script src="//cdn.datatables.net/1.10.22/js/jquery.dataTables.min.js"></script>

    <script>
        $(document).ready(function () {
            $('.table').DataTable({
                "order": [[0, "asc"]]
            });
        });
    </script>
}